
Flask==2.3.2
Werkzeug==2.3.4
Jinja2==3.1.2
itsdangerous==2.1.2
click==8.1.3
gunicorn==20.1.0
python-dotenv==1.0.0

Flask-SQLAlchemy==3.0.3
Flask-Migrate==4.0.4
Flask-CORS==3.0.10
whitenoise==6.4.0

# Yes, this application has a database. The presence of 'psycopg2' and 'dj-database-url' in the requirements.txt file indicates that the application is using a PostgreSQL database.

# psycopg2 is a PostgreSQL adapter for Python
# dj-database-url is used to configure the database connection using a URL

# To run the application locally on port 8080, you don't need to modify the requirements.txt file.
# Instead, you can use the following command in your terminal:
#!/usr/bin/env python


psycopg2-binary==2.9.6

flask_login
